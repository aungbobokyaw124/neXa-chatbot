{
  "name": "nexa-worker",
  "version": "1.0.0",
  "private": true,
  "description": "neXa AI Cloudflare Worker backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
