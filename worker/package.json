{
  "name": "nexa-worker",
  "version": "1.0.0",
  "description": "neXa AI — Cloudflare Worker backend",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev --local",
    "deploy": "wrangler deploy",
    "tail":   "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
